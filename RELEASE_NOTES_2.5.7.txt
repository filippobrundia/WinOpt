WinOpt 2.5.7 - RELEASE DEFINITIVA

Base: WinOpt_2.5.7_HF1 (Claude release package) + consolidamento.

Fix strutturali:
- LAUNCHER.cmd: line endings normalizzati (CRLF), eliminato CRCRLF.
- UAC quoting: basato su %~f0 (robusto con spazi/parentesi).
- RUNADMIN/RUNUSER: consolidati su RUNSCRIPT con enforcement (net session) per ADMIN.
- Assert-Admin: funzione in Modules\_COMMON\Common.ps1 produce messaggio chiaro + exit 1 (niente throw).
- Assert-Admin: presente in tutti i moduli Admin come safety net per lancio diretto (exit 1 con messaggio). Centralizzata in Common.ps1, non duplicata.
- Versioning: aggiornati README/Docs/Launcher a 2.5.7.
- Aggiunto MANIFEST_SHA256.txt per integrità bit-per-bit della release.

Fix post-release (2.5.7-p1):
- ONEDRIVE_MENU: riscritto con label esplicite (:OD_ON, :OD_OFF, :OD_VERIFY).
  Il pattern (call :RUNADMIN ... & goto :EOF) dentro blocco IF causava chiusura
  anticipata della finestra senza mostrare la schermata POST con i risultati.
- MANIFEST_SHA256.txt: aggiornato per includere i file corretti post-patch.

